@model List<CatModel>

<div>@Model.Count() cats in the system!</div>

@if (Model.Any())
{
    @foreach (var cat in Model)
    {
        <div>@cat.Name is @cat.Age years old and the owner is @cat.Owner</div>
    }
}
else
{
    <div>No cats in the system!</div>
}
